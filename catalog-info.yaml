apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: rust_jenkins_test
  annotations:
    backstage.io/kubernetes-id: "users-api"
    backstage.io/kubernetes-connects-to: "orders-api"
spec:
  type: service
  lifecycle: experimental
  owner:  guests